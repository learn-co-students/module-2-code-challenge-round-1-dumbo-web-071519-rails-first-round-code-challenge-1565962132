<%= if flash[:errors] %>
<%= flash[:errors].each do |error| %>


<%= form_for @vendorsweets do |vs|%>
    <%= f.label :price %>
    <%= f.number_field :price %>
    
    <%= f.label :sweet_id %>
    <%= f.collection_select :sweet_id, Sweet.all, :id, :name %>

    <%= f.label :vendor_id %>
    <%= f.collection_select :vendor_id, Vendor.all, :id, :name %>

    <%= f.submit %>

<%= end %>